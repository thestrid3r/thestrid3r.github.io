---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<span class="px-4 py-2 rounded-full text-base font-semibold bg-white/70 dark:bg-gray-800/70 shadow-md border border-transparent hover:border-gradient-to-r hover:from-blue-400 hover:to-orange-400 transition-all duration-200 backdrop-blur-md text-gray-900 dark:text-gray-100 hover:scale-105 hover:shadow-lg cursor-pointer select-none">
  {name}
</span> 